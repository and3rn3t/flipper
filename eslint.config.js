// Flipper Zero Lab — lint config.
// Uses the shared @and3rn3t/eslint-config (ESLint 9 flat config). To add
// repo-specific rules, spread it: export default [...and3rn3t, { rules: {...} }].
import and3rn3t from '@and3rn3t/eslint-config'

export default and3rn3t
