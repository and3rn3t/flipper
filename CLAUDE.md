# CLAUDE.md — Flipper Zero Lab

**Read `AGENTS.md` first — it is the single source of truth for stack, commands, and conventions.** This file only holds Claude-specific additions.

## Claude-specific notes

- Run `npm run lint && npm run type-check && npm test` before declaring a change done.
- Prefer the Cloudflare MCP connector (Cowork sessions) for inspecting the Pages project over guessing from config.
